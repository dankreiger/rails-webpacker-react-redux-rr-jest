import React from 'react';
import { HomeContainer } from './Home.styles';

const Home = () => {
  return (
    <HomeContainer>
      Starter with RoR, React, Redux, React-Router, RSpec, and Jest
    </HomeContainer>
  );
};

export default Home;
